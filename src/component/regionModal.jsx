import React from 'react';
import {Card} from "antd";

function RegionModal(name) {
    const [modalOpen, setModalOpen] = React.useState(false);
    console.log(name);
    return (
        <>
            <Card>
                dddd
            </Card>
        </>
    );
}

export default RegionModal;